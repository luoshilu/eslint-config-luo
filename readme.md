## eslint-config-luo

### usage

Add the extension:

```
// .eslintrc.js
module.exports = {
   extends: "luo"
}

// OR

// .eslintrc
{
   "extends": "luo"
}
```

Then install the config:

```
npm i eslint-config-luo --save-dev
```